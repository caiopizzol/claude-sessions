pre-commit:
  parallel: true
  commands:
    swiftlint:
      glob: "*.swift"
      run: swiftlint lint --quiet widget/
    swiftformat:
      glob: "*.swift"
      run: swiftformat --lint widget/
    build:
      glob: "*.swift"
      run: cd widget && swift build -c debug
